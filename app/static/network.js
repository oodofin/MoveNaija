/* Extra network UI. Precise user coordinates stay in memory only. */
let activeCoords=null, map=null, mapLayer=null, previousOptions=[],lastMapResult=null;
const km=m=>m>=1000?(m/1000).toFixed(1)+' km':m+' m';
const fareText=o=>o.fare_min==null?'Fare estimate unavailable':o.fare_min===o.fare_max?formatFare(o.fare_min):`${formatFare(o.fare_min)} – ${formatFare(o.fare_max)}`;
const durationText=o=>o.duration_min==null?'Duration estimate unavailable':`About ${o.duration_min} min (estimate)`;
async function loadPublishedFares(origin,destination){const section=$('#published-fares-section');section.hidden=true;$('#published-fares').innerHTML='';if(origin==='Current location')return;
 try{const rows=await api('/fares/published?'+new URLSearchParams({origin,destination}));if(!rows.length)return;section.hidden=false;
 $('#published-fares').innerHTML=rows.map(r=>`<article class="route-card"><span class="transport-badge">${escapeHtml(r.mode==='Standard'?'Standard bus':'BRT')}</span><h3>${escapeHtml(r.label)}</h3><div class="route-facts"><span><strong>${r.published_amounts.length===1?formatFare(r.published_amounts[0]):'Conflicting listed amounts'}</strong><br>LAMATA published fare, ${escapeHtml(r.effective_date)}</span></div><p>${r.direction==='reverse_listing'?'LAMATA lists this corridor in the opposite order. Return service and fare are not confirmed.':'LAMATA lists this endpoint corridor. Exact boarding stop, intermediate stops and transfers are not supplied.'}</p><a class="outline-button" href="${escapeHtml(r.source_url)}" target="_blank" rel="noopener noreferrer">Check LAMATA source ↗</a></article>`).join('')}
 catch(e){section.hidden=false;$('#published-fares').innerHTML=empty('Fare references unavailable',e.message)}}
function journeyCard(o,i){
 const steps=[];
 if(o.walk_to_board_m!=null)steps.push(`Walk approximately ${km(o.walk_to_board_m)} to ${o.segments[0].board_stop.name}. Straight-line estimate; actual walking route may differ.`);
 else steps.push(`Go to ${o.segments[0].board_stop.name}.`);
 o.segments.forEach((s,n)=>{
   if(n)steps.push(`Change transport at ${s.board_stop.name}.`);
   steps.push(`Take ${s.mode} toward ${s.towards} from ${s.board_stop.name}. Get down at ${s.alight_stop.name}.`);
 });
 steps.push(o.walk_to_destination_m!=null?`Destination is approximately ${km(o.walk_to_destination_m)} from ${o.segments.at(-1).alight_stop.name} (straight-line distance; check a walking route).`:'Walk from the final stop to your destination. Check the local walking route before travelling.');
 return `<article class="route-card network-card"><div class="route-main"><div><span class="transport-badge">OPTION ${i+1}</span><h3>${escapeHtml(o.origin)} → ${escapeHtml(o.destination)}</h3><p>${o.segments.map(s=>escapeHtml(s.mode)).join(' + ')} · ${o.transfers} ${o.transfers===1?'transfer':'transfers'}</p></div><span class="source-badge ${o.all_verified?'verified':''}">${o.all_verified?'Source checked':'Partly unverified'}</span></div><div class="route-facts"><span><strong>${escapeHtml(fareText(o))}</strong><br>total estimated fare</span><span><strong>${escapeHtml(durationText(o))}</strong><br>estimated journey time</span>${o.walk_to_board_m!=null?`<span><strong>${km(o.walk_to_board_m)}</strong><br>straight-line to boarding</span>`:''}</div><h4>How to get there</h4><ol class="journey-steps">${steps.map(step=>`<li>${escapeHtml(step)}</li>`).join('')}</ol><div class="source-details">${o.segments.map(s=>`<div><b>${escapeHtml(s.mode)} · Route #${s.route_id}</b> · ${escapeHtml(s.via.join(' → '))}. ${s.verified?'Published route':'Information not verified'}${s.source_url?` · <a href="${escapeHtml(s.source_url)}" target="_blank" rel="noopener noreferrer">Source ↗</a>`:''}</div>`).join('')}</div><p class="caution">Fares may change depending on time, operator and transport conditions. Walking distance is straight-line only. Confirm service locally.</p><div class="route-actions"><button class="outline-button" type="button" data-map-journey="${i}">Optional map</button><button class="outline-button" type="button" data-fare-route="${o.segments[0].route_id}" data-fare-mode="${escapeHtml(o.segments[0].mode)}">Report current fare</button><button class="plain-button" type="button" data-route-report="${o.segments[0].route_id}" data-route-name="${escapeHtml(o.origin+' to '+o.destination)}">Report route update</button></div></article>`;
}
function renderJourneys(){let options=[...previousOptions],sort=$('#journey-sort').value;
 const menu=$('#journey-sort');
 for(const option of menu.options){
   option.disabled=option.value==='fare'?options.some(x=>x.fare_min==null):option.value==='duration'?options.some(x=>x.duration_min==null):option.value==='walking'?options.some(x=>x.walk_to_board_m==null):false;
 }
 if(menu.selectedOptions[0]?.disabled){sort='transfers';menu.value=sort}
 if(sort==='fare' && options.every(x=>x.fare_min!=null))options.sort((a,b)=>a.fare_min-b.fare_min);
 else if(sort==='duration' && options.every(x=>x.duration_min!=null))options.sort((a,b)=>a.duration_min-b.duration_min);
 else if(sort==='walking' && options.every(x=>x.walk_to_board_m!=null))options.sort((a,b)=>a.walk_to_board_m-b.walk_to_board_m);
 else options.sort((a,b)=>a.transfers-b.transfers);
 $('#journey-sort').value=sort;
 $('#journey-results').innerHTML=options.length?options.map(journeyCard).join(''):empty('No reviewed connection for this journey','Check nearby stops or share what you know.','#reports','Know how to get there?');
 window.displayedJourneys=options;
}
async function loadJourneys(origin,destination){
 const searchCoords=origin==='Current location' && activeCoords;
 const params=new URLSearchParams({destination, ...(searchCoords?{lat:activeCoords.latitude,lon:activeCoords.longitude}:{origin})});
 $('#journey-results').innerHTML=empty('Finding journey options','Checking known transport connections…');
 try{const result=searchCoords?await api('/journeys',{method:'POST',body:JSON.stringify({lat:activeCoords.latitude,lon:activeCoords.longitude,destination})}):await api('/journeys?'+params);previousOptions=result.options;lastMapResult=result;renderJourneys();if(result.reason){const stops=(result.nearby_destination_stops||[]).slice(0,5);$('#journey-results').innerHTML=empty(result.status==='place_not_found'?'Location not found':result.status==='no_nearby_stops'?'Transport data unavailable':'No documented public transport journey yet',result.reason+' A complete-journey fare is unavailable without a documented connection.','#reports','Know how to get there?')+(result.destination_place?`<p class="caution">Found: ${escapeHtml(result.destination_place.display_name)} · OpenStreetMap</p>`:'')+stops.map(s=>`<div class="stop-card"><strong>${escapeHtml(s.name)}</strong><small>${s.distance_m!=null?km(s.distance_m)+' straight-line from destination':'Nearby mapped stop'} · ${escapeHtml(s.source||'unverified')}</small></div>`).join('')};if($('#journey-map-details').open)renderMap(result.options[0],result);}
 catch(e){previousOptions=[];lastMapResult=null;$('#journey-results').innerHTML=empty('Could not plan this journey',e.message)}
}
$('#journey-sort').addEventListener('change',renderJourneys);
function mapMarkerText(type){return {bus:'🚌',brt:'🚌',keke:'🛺',rail:'🚆',ferry:'⛴️',other:'📍',user:'📍',destination:'🏁'}[type]||'📍'}
function marker(lat,lon,type,label){return L.marker([lat,lon],{icon:L.divIcon({className:'stop-marker',html:`<span>${mapMarkerText(type)}</span>`,iconSize:[32,32],iconAnchor:[16,16]})}).bindPopup(escapeHtml(label))}
function ensureMap(){if(map)return true;if(typeof L==='undefined'){$('#map-message').textContent='Map library could not load. Journey steps remain available above.';return false}map=L.map('lagos-map',{scrollWheelZoom:false}).setView([6.52,3.38],11);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{attribution:'© OpenStreetMap contributors',maxZoom:19}).addTo(map);mapLayer=L.layerGroup().addTo(map);return true}
function renderMap(option,result){if(!$('#journey-map-details').open||!ensureMap())return;mapLayer.clearLayers();const points=[];
 const origin=result?.origin_place,destination=result?.destination_place;
 if(origin?.latitude!=null){marker(origin.latitude,origin.longitude,'user',origin.display_name).addTo(mapLayer);points.push([origin.latitude,origin.longitude])}
 if(destination?.latitude!=null){marker(destination.latitude,destination.longitude,'destination',destination.display_name).addTo(mapLayer);points.push([destination.latitude,destination.longitude])}
 for(const s of [...(result?.nearby_origin_stops||[]),...(result?.nearby_destination_stops||[])])if(s.latitude!=null){marker(s.latitude,s.longitude,s.stop_type,s.name).addTo(mapLayer);points.push([s.latitude,s.longitude])}
 const segments=option?.segments||[];
 if(segments.length){segments.forEach((s,index)=>{const pts=[];s.path_stops.forEach((stop,j)=>{const name=stop.name;if(stop.latitude!=null){const p=[stop.latitude,stop.longitude];pts.push(p);points.push(p);marker(...p,stop.stop_type||'bus',name).addTo(mapLayer)}});if(pts.length>1)L.polyline(pts,{color:index%2?'#df9b38':'#13886a',weight:5,dashArray:'7,7'}).addTo(mapLayer)});
 $('#map-message').textContent='Lines connect known station coordinates schematically. Missing coordinates are not placed on the map.';
 }else{$('#map-message').textContent='Known stops appear on this map when coordinates are available. No route geometry is claimed.'}
 if(points.length)map.fitBounds(L.latLngBounds(points).pad(.25),{maxZoom:14});setTimeout(()=>map.invalidateSize(),100);
}
async function showNearby(coords){try{const stops=await api('/stops/nearby',{method:'POST',body:JSON.stringify({lat:coords.latitude,lon:coords.longitude})});$('#nearby-home').innerHTML=stops.length?stops.map(s=>`<div class="stop-card"><strong>${mapMarkerText(s.stop_type)} ${escapeHtml(s.name)}</strong><small>${km(s.distance_m)} straight-line · ${escapeHtml(s.transport_types)} · ${s.verified?'Source checked':'Unverified stop'}</small><span>${s.routes_served.length?s.routes_served.map(r=>escapeHtml(r.destination)).join(', '):'No reviewed connections linked yet'}</span></div>`).join(''):empty('No mapped stops nearby yet','Nearby search needs transport stops with verified coordinates. Enter an origin manually.');if(stops.length&&$('#journey-map-details').open&&ensureMap()){mapLayer.clearLayers();const pts=[];stops.forEach(s=>{marker(s.latitude,s.longitude,s.stop_type,s.name).addTo(mapLayer);pts.push([s.latitude,s.longitude])});marker(coords.latitude,coords.longitude,'user','Your approximate location').addTo(mapLayer);pts.push([coords.latitude,coords.longitude]);map.fitBounds(L.latLngBounds(pts).pad(.2),{maxZoom:14})}}
 catch(e){$('#nearby-home').innerHTML=empty('Could not find nearby stops',e.message)}}
function requestLocation(purpose){if(!navigator.geolocation){toast('Location is unavailable. Enter your starting point manually.');return}navigator.geolocation.getCurrentPosition(position=>{activeCoords={latitude:position.coords.latitude,longitude:position.coords.longitude};if(purpose==='nearby'){showNearby(activeCoords);location.hash='home'}else{const input=purpose==='home'?$('#origin'):$('#route-origin');input.value='Current location';const destination=purpose==='home'?$('#destination').value:$('#route-destination').value;if(destination.trim().length>=2)search('Current location',destination);else toast('Location ready. Enter your destination to find a route.')}},()=>{activeCoords=null;toast('We couldn’t access your location. Enter your starting point manually.')},{enableHighAccuracy:false,timeout:12000,maximumAge:60000})}
document.querySelectorAll('[data-locate]').forEach(b=>b.addEventListener('click',()=>requestLocation(b.dataset.locate)));
for(const el of ['#origin','#route-origin'])$(el).addEventListener('input',e=>{if(e.target.value!=='Current location')activeCoords=null});
let suggestionTimer;
for(const el of ['#origin','#destination','#route-origin','#route-destination'])$(el).addEventListener('input',e=>{clearTimeout(suggestionTimer);const q=e.target.value.trim();if(q.length<3)return;suggestionTimer=setTimeout(async()=>{try{const options=await api('/locations/suggest?'+new URLSearchParams({q}));$('#known-locations').innerHTML=options.map(x=>`<option value="${escapeHtml(x.name)}">${escapeHtml(x.area||x.location)}</option>`).join('')}catch{}},350)});
async function loadTransportData(){if(!user||user.role!=='admin')return;try{const q=$('#transport-filter').value,source=$('#transport-source').value;
 const [stops,routes]=await Promise.all([api('/admin/transport/stops?'+new URLSearchParams({q,source})),api('/admin/transport/routes?'+new URLSearchParams({q,source}))]);
 $('#transport-data').innerHTML=`<h3>Stops (${stops.length} shown)</h3>`+stops.map(s=>`<div class="stack-item"><strong>#${s.id} ${escapeHtml(s.name)}</strong><small>${escapeHtml(s.source||'sample')} · ${s.verified?'verified':'discovered / unverified'} · ${escapeHtml(s.status)} · synced ${escapeHtml(s.last_synced_at||'never')}</small><div class="route-actions"><button class="outline-button" data-transport="verify" data-kind="stops" data-id="${s.id}">Verify</button><button class="plain-button" data-transport="deactivate" data-kind="stops" data-id="${s.id}">Deactivate</button><button class="plain-button" data-transport="merge" data-id="${s.id}">Merge into another ID</button></div></div>`).join('')+`<h3>Routes (${routes.length} shown)</h3>`+routes.map(r=>`<div class="stack-item"><strong>#${r.id} ${escapeHtml(r.name||r.origin+' → '+r.destination)}</strong><small>${escapeHtml(r.source)} · ${r.verified?'verified':'unverified'} · synced ${escapeHtml(r.last_synced_at||'never')}</small><div class="route-actions"><button class="outline-button" data-transport="verify" data-kind="routes" data-id="${r.id}">Verify</button><button class="plain-button" data-transport="deactivate" data-kind="routes" data-id="${r.id}">Deactivate</button></div></div>`).join('')}
 catch(e){$('#transport-data').textContent=e.message}}
$('#transport-refresh').onclick=loadTransportData;
$('#route-discovery-form').onsubmit=async e=>{e.preventDefault();if(!user)return openAuth();const data=Object.fromEntries(new FormData(e.target));data.approximate_fare=data.approximate_fare?Number(data.approximate_fare):null;try{await api('/reports/routes',{method:'POST',body:JSON.stringify(data)});e.target.reset();toast('Suggestion sent for review. It is not a verified route.');loadRouteDiscoveries()}catch(err){toast(err.message)}};
async function loadRouteDiscoveries(){if(!user)return;try{const rows=await api('/me/route-discoveries');$('#my-route-discoveries').innerHTML=rows.map(r=>`<div class="stack-item">${escapeHtml(r.starting_stop)} → ${escapeHtml(r.destination_stop)} · ${escapeHtml(r.status)}</div>`).join('')}catch{}}
async function loadAdminDiscoveries(){if(!user||user.role!=='admin')return;try{const rows=await api('/admin/route-discoveries');$('#admin-route-discoveries').innerHTML=rows.map(r=>`<div class="stack-item"><strong>${escapeHtml(r.starting_stop)} → ${escapeHtml(r.destination_stop)} · ${escapeHtml(r.status)}</strong><p>${escapeHtml(r.transport_type)} · ${escapeHtml(r.transfer_details)} · ${escapeHtml(r.instructions)}</p>${r.status==='pending'?`<button class="outline-button" data-review-discovery="approved" data-id="${r.id}">Mark reviewed</button><button class="plain-button" data-review-discovery="rejected" data-id="${r.id}">Reject</button>`:''}</div>`).join('')}catch{}}
document.addEventListener('click',async e=>{const b=e.target.closest('[data-review-discovery]');if(!b)return;try{await api('/admin/route-discoveries/'+b.dataset.id,{method:'PATCH',body:JSON.stringify({status:b.dataset.reviewDiscovery})});toast('Review saved. This did not publish a route.');loadAdminDiscoveries()}catch(err){toast(err.message)}});
$('#transport-import-form').onsubmit=async e=>{e.preventDefault();try{const rows=JSON.parse($('#transport-import-json').value);const result=await api('/admin/transport/import',{method:'POST',body:JSON.stringify(rows)});toast(`Imported ${result.ids.length} reviewed routes.`);await loadTransportData();loadAdmin()}catch(err){toast(err.message)}};
document.addEventListener('click',async e=>{const b=e.target.closest('[data-transport]');if(!b)return;try{
 if(b.dataset.transport==='merge'){const keep=prompt('ID of the stop to keep:');if(!keep)return;await api('/admin/transport/stops/merge',{method:'POST',body:JSON.stringify({keep_id:Number(keep),duplicate_id:Number(b.dataset.id)})})}
 else{let source_url='';if(b.dataset.transport==='verify'){source_url=prompt('Trusted HTTPS source URL used to verify this record:')||'';if(!source_url)return}
 await api(`/admin/transport/${b.dataset.kind}/${b.dataset.id}`,{method:'PATCH',body:JSON.stringify({action:b.dataset.transport,source_url})})}
 toast('Transport record updated.');loadTransportData();loadAdmin()}
 catch(err){toast(err.message)}});
$('#journey-map-details').addEventListener('toggle',()=>{if($('#journey-map-details').open)renderMap(lastMapResult?.options?.[0],lastMapResult)});
document.addEventListener('click',e=>{const mapButton=e.target.closest('[data-map-journey]');if(mapButton){$('#journey-map-details').open=true;renderMap(window.displayedJourneys[Number(mapButton.dataset.mapJourney)],lastMapResult);$('#journey-map-details').scrollIntoView({behavior:'smooth'})}
 const fare=e.target.closest('[data-fare-route]');if(fare){location.hash='reports';showPage('reports');$('#fare-report-form').elements.route_id.value=fare.dataset.fareRoute;$('#fare-report-form').elements.transport_type.value=fare.dataset.fareMode;$('#fare-report-form').scrollIntoView({behavior:'smooth'})}
 const update=e.target.closest('[data-route-report]');if(update){location.hash='reports';showPage('reports');$('#report-location').value=update.dataset.routeName;$('#report-form').dataset.routeId=update.dataset.routeReport}
 const review=e.target.closest('[data-action="review-fare"]');if(review)setTimeout(loadFareReports,400)
});
$('#fare-report-form').elements.paid_on.value=new Date().toISOString().slice(0,10);
$('#fare-report-form').addEventListener('submit',async e=>{e.preventDefault();if(!user)return openAuth();const data=Object.fromEntries(new FormData(e.target));data.route_id=Number(data.route_id);data.amount=Number(data.amount);try{await api('/fare-reports',{method:'POST',body:JSON.stringify(data)});e.target.reset();toast('Fare report sent for review. Published fares have not changed.');loadFareReports()}catch(err){toast(err.message)}});
async function loadFareReports(){if(!user){$('#my-fare-reports').innerHTML='';return}try{const rows=await api('/me/fare-reports');$('#my-fare-reports').innerHTML=rows.map(r=>`<div class="stack-item"><strong>Route #${r.route_id}: ${formatFare(r.amount)} · ${escapeHtml(r.status)}</strong><small>Paid ${escapeHtml(r.paid_on)}</small></div>`).join('')}catch{}}
window.addEventListener('hashchange',()=>{if(location.hash==='#reports'){loadFareReports();loadRouteDiscoveries()}if(location.hash==='#profile'&&user?.role==='admin'){loadTransportData();loadAdminDiscoveries()}if(location.hash==='#routes'&&map)setTimeout(()=>map.invalidateSize(),100)});
